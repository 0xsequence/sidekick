export const PRISMA_NOT_INITIALIZED =
	'Prisma client not initialized, there is no DATABASE_URL environment variable set. In order to use any database features, you need to set the DATABASE_URL environment variable.'
